NODE_ENV=stage
PORT=3000

# Docker: defina DOCKER_CONTAINER=true no docker-compose; use host "postgres" no serviço.
DOCKER_CONTAINER=false

DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=escala
DB_PASSWORD=escala
DB_DATABASE=escala_agro
